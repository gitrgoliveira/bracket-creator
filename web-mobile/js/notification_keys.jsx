// Shared localStorage key for the browser notification opt-in flag.
// Both viewer.jsx and app.jsx import this so a rename always stays in sync.
export const LS_NOTIFICATIONS_ENABLED = "viewer.notifications.enabled";
